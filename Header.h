#pragma once
#include <iostream>
#include <fstream>
#include <conio.h>
#include <cstdio> 
#include <string>
using namespace std;

void createFile();
void readFile();
void showMap(char** map, int x, int y);
void quickSort(int *a, int min, int max);